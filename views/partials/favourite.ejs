<form action="/favourites" method="POST">
  <button
    type="submit"
    class="w-full bg-green-300 font-bold py-2.5 px-4 rounded-lg hover:bg-green-500 transition duration-300 flex items-center justify-center gap-2"
  >
    <span>➕</span> Add to Favourite
  </button>
  <input type="hidden" name="id" value="<%= home._id %>" />
</form>
